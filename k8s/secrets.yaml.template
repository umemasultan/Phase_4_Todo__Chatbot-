# This is a template file. Create actual secrets using kubectl commands.
# DO NOT commit actual secrets to version control.

# To create secrets, run:
# kubectl create secret generic app-secrets \
#   --from-literal=CLAUDE_API_KEY='your-claude-api-key' \
#   --from-literal=JWT_SECRET='your-jwt-secret' \
#   --from-literal=DB_USER='postgres' \
#   --from-literal=DB_PASSWORD='your-db-password' \
#   --from-literal=DATABASE_URL='postgresql://postgres:your-db-password@postgres-service:5432/todo_chatbot?schema=public' \
#   -n todo-chatbot

# Or use this template and replace values:
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: todo-chatbot
type: Opaque
stringData:
  CLAUDE_API_KEY: "REPLACE_WITH_YOUR_CLAUDE_API_KEY"
  JWT_SECRET: "REPLACE_WITH_GENERATED_SECRET"
  DB_USER: "postgres"
  DB_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  DATABASE_URL: "postgresql://postgres:REPLACE_WITH_SECURE_PASSWORD@postgres-service:5432/todo_chatbot?schema=public"
